{
  "name": "intro-component-with-signup-form",
  "version": "1.0.0",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html",
    "predeploy": "del /q dist && parcel build src/index.html",
    "deploy": "gh-pages -d dist"
  },
  "license": "MIT",
  "devDependencies": {
    "parcel": "^2.4.1",
    "gh-pages": "^3.2.3"
  }
}
